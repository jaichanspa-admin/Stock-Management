// ============================================================================
// JAI CHAN SPA — Stock Management : Supabase connection config
// ----------------------------------------------------------------------------
// Paste your project's values below (Supabase → Project Settings → API).
// The anon (publishable) key is SAFE to expose publicly — Row Level Security
// in the database is what actually protects your data. Do NOT put the
// "service_role" / secret key here.
// ============================================================================
window.JAICHAN_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-ref.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY"
};
